# Copy to backend/.env

PORT=5000
FRONTEND_URL=http://localhost:3000

# Local: place service-account.json in backend/
# Render/production: set FIREBASE_SERVICE_ACCOUNT to the full JSON (one line)
# Optional: GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
