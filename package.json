{
  "name": "mini-hcm",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "npx concurrently -n api,web -c blue,green \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "install:all": "npm install && npm install --workspace=frontend && npm install --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "deploy:rules": "npx firebase-tools deploy --only firestore:rules,firestore:indexes",
    "deploy:hosting": "npm run build:frontend && npx firebase-tools deploy --only hosting"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "firebase-tools": "^13.0.0"
  }
}
